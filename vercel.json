{
  "buildCommand": "cd frontend/pda-ui && npm install && npm run build",
  "outputDirectory": "frontend/pda-ui/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

